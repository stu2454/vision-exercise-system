/**
 * Version identifiers, mirroring src/version.py.
 *
 * Kept in step deliberately: a recording made in the browser and one made in
 * Python must declare the same format version, or the reader cannot treat
 * them alike.
 */

export const APPLICATION_VERSION = "0.1.0";
export const POSE_STREAM_FORMAT_VERSION = "0.2";
